---
pagination:
  data: cateringProducts.items
  size: 1
  alias: product
permalink: /caterin/{{ product.fields.name | slugify }}/index.html
layout: base.njk
eleventyComputed:
  title: "{{ product.fields.name }} | Pepper Farm Deli"
  description: "The {{ product.fields.name }} is a {{ product.fields.description }}."
---

<script type="application/ld+json">
  {% from "partials/product-schema-macro.njk" import productSchema %}
  {{ productSchema(product) }}
</script>

<div class="section mb-4">
  <div class="flex flex-col md:flex-row md:mt-8">
    <h2 class="md:hidden text-center font-bold text-3xl my-8">{{ product.fields.name }}</h2>
    
    {%- if product.fields.images | length > 0 -%}

      {%- for image in product.fields.images -%}
        <img 
          class="h-[460px]  md:h-[640px] object-cover w-full md:w-1/2 md:pl-4 xl:pl-0" 
          eleventy:widths="639"
          src="{{ image.fields.file.url | cfimg }}" 
          alt="{{ product.fields.description }}"/>
      {%- endfor -%}

    {%- else -%}
        <img 
          class="h-[400px] object-cover w-full md:w-1/2" 
          src="/assets/img/missing_image.webp" 
          alt="This image is missing"/>
    {%- endif -%}

    <div class="flex flex-col my-8 md:my-0 md:w-1/2 px-4 lg:px-16 text-center">
      <h2 class="hidden md:block text-center font-bold text-3xl md:mb-8">{{ product.fields.name }}</h2>
      <p class="text-xl mb-8 px-4 md:px-0">{{ product.fields.description }}</p>
      <div class="flex flex-row justify-center gap-8 mb-8">
        <div class="text-5xl font-bold">$ {{ product.fields.price }}</div>
        {%- if product.fields.orderLink -%}
        <a onclick="window.dataLayer.push({'event': 'order_started_event'});" id="order-product-page" class="solid-button" href="{{ product.fields.orderLink }}">Order</a>
        {%- endif -%}
      </div>
    </div>

  </div>
 
</div>